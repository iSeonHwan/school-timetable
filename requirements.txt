# ── 공통 (기존) ────────────────────────────────────────────────────────────
PyQt6>=6.4.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
reportlab>=4.0.0
openpyxl>=3.1.0

# ── 서버 (server/) ─────────────────────────────────────────────────────────
fastapi>=0.111.0          # API 서버 프레임워크
uvicorn[standard]>=0.29.0 # ASGI 서버 (WebSocket 지원 포함)
python-jose[cryptography]>=3.3.0  # JWT 생성·검증
passlib[bcrypt]>=1.7.4    # 비밀번호 해싱 (bcrypt)
pydantic>=2.0.0           # 데이터 검증 스키마 (FastAPI 내장)

# ── 클라이언트 앱 (admin_app/, teacher_app/) ───────────────────────────────
httpx>=0.27.0             # 동기 HTTP 클라이언트 (ApiClient)
websocket-client>=1.8.0   # WebSocket 클라이언트 (채팅)

# ── 개발 / 테스트 ──────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-qt>=4.4.0
httpx>=0.27.0             # pytest 에서 FastAPI TestClient 용도로도 사용

# ── 빌드 / 인스톨러 ────────────────────────────────────────────────────────
pyinstaller>=6.0.0
Pillow>=11.0.0
dmgbuild>=1.6.0           # macOS DMG (macOS only)
